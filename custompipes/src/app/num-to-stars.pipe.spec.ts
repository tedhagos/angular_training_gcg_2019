import { NumToStarsPipe } from './num-to-stars.pipe';

describe('NumToStarsPipe', () => {
  it('create an instance', () => {
    const pipe = new NumToStarsPipe();
    expect(pipe).toBeTruthy();
  });
});
